# Backend Environment Variables
# CRITICAL: Never commit this file to Git!

# Database Configuration
DATABASE_URL=postgresql://postgres:arohan_secure_2026@localhost:5432/arohan_health_db
DB_PASSWORD=arohan_secure_2026
POSTGRES_DB=arohan_health_db

# Security - JWT
# IMPORTANT: Generate a new secret for production using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=9774874f6315f661b65cef75afcdd73d7df7dd392bbd3b96465bdb3b769c6dfe
JWT_EXPIRE=7d

# Server Configuration
NODE_ENV=development
PORT=5000

# CORS Configuration (comma-separated list of allowed origins)
# Add your production domain here
ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# Logging
LOG_LEVEL=info

# Database Pool Configuration
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT=30000

# Razorpay Payment Gateway (Test Credentials)
# For production, replace with actual Razorpay API keys from https://dashboard.razorpay.com/
RAZORPAY_KEY_ID=rzp_test_1aeb96771bd3a43a5cb82401
RAZORPAY_KEY_SECRET=3187d450123270110537c29051e8d9c3
RAZORPAY_WEBHOOK_SECRET=whsec_f642b6287e8d5623ba68b87c0b6666825daa0cc56

# Email Configuration (SMTP)
# Development/Gmail Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@haspranahealth.com
SMTP_PASSWORD=Arohan@2026!Health
ADMIN_EMAIL=info@haspranahealth.com

# Google reCAPTCHA v3
# Get your keys from https://www.google.com/recaptcha/admin
# IMPORTANT: Replace with your actual keys for production
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here

# Encryption
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5174
