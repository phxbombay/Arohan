# ===================================
# PRODUCTION ENVIRONMENT VARIABLES
# Arohan Health Platform
# ===================================
# IMPORTANT: Fill in all values before deployment
# Generate secrets using: openssl rand -base64 64

# ===================================
# NODE ENVIRONMENT
# ===================================
NODE_ENV=production

# ===================================
# DATABASE CONFIGURATION
# ===================================
POSTGRES_DB=arohan_health_db
POSTGRES_USER=arohan_prod
# CHANGE THIS: Use openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_32_CHAR_PASSWORD

# Database URL for Backend
# Match the password above
DATABASE_URL=postgresql://arohan_prod:CHANGE_ME_32_CHAR_PASSWORD@postgres:5432/arohan_health_db

# Or use this if keeping 'postgres' as user:
# DATABASE_URL=postgresql://postgres:CHANGE_ME_32_CHAR_PASSWORD@postgres:5432/arohan_health_db

# ===================================
# BACKEND API CONFIGURATION
# ===================================
PORT=5000

# JWT Secret - CRITICAL: Generate with openssl rand -base64 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_64

# ===================================
# CORS CONFIGURATION
# ===================================
# Add your production domain(s)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com

# ===================================
# ADMIN CONFIGURATION
# ===================================
ADMIN_EMAIL=admin@yourdomain.com

# ===================================
# SMTP EMAIL CONFIGURATION (Production)
# ===================================
# Option 1: Gmail (Recommended for small scale)
# Follow setup guide: gmail_smtp_setup.md
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password

# Option 2: SendGrid (Recommended for scale)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=SG.your-sendgrid-api-key

# Option 3: AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-ses-smtp-username
# SMTP_PASSWORD=your-ses-smtp-password

# ===================================
# TWILIO SMS CONFIGURATION (Production)
# ===================================
# Get these from https://console.twilio.com
# Follow setup guide: twilio_sms_setup.md
TWILIO_ACCOUNT_SID=ACXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
TWILIO_AUTH_TOKEN=your_twilio_auth_token_32_chars
TWILIO_PHONE_NUMBER=+1234567890

# SMS Rate Limiting (messages per minute)
SMS_RATE_LIMIT_PER_MINUTE=5

# ===================================
# PAYMENT GATEWAY (Razorpay)
# ===================================
# Production keys from https://dashboard.razorpay.com
RAZORPAY_KEY_ID=rzp_live_XXXXXXXXXXXX
RAZORPAY_KEY_SECRET=your_production_secret_key
RAZORPAY_WEBHOOK_SECRET=your_webhook_signing_secret

# ===================================
# FRONTEND CONFIGURATION
# ===================================
# API URL that frontend will use
VITE_API_URL=https://api.yourdomain.com

# Or if using same domain:
# VITE_API_URL=https://yourdomain.com

# ===================================
# OPTIONAL: LOGGING & MONITORING
# ===================================
# LOG_LEVEL=info
# SENTRY_DSN=your_sentry_dsn_for_error_tracking

# ===================================
# SECURITY NOTES
# ===================================
# 1. Never commit this file to git
# 2. Use different passwords for each environment
# 3. Rotate secrets regularly
# 4. Use strong passwords (minimum 32 characters)
# 5. Enable 2FA on all service accounts
# 6. Regularly audit access logs

# ===================================
# QUICK COMMANDS TO GENERATE SECRETS
# ===================================
# JWT Secret: openssl rand -base64 64
# DB Password: openssl rand -base64 32
# Webhook Secret: openssl rand -hex 32
